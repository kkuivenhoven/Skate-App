<% provide(:title, 'Skate Feed') %>
<% provide(:p, 'Your Skate Feed') %>

<% if logged_in? %>
  <div class="container">
					<div class="row">
						<aside class="col-md-4 stats_micro_form">
							<section class="user_info">
								<%= render 'shared/user_info' %>
							</section>
							<section class="stats">
								<%= render 'shared/stats' %>
							</section>
							<section class="micropost_form">
								<%= render 'shared/micropost_form' %>
							</section>
						</aside>
					</td>
					<td>
						<div class="col-md-7 news_feed_posts">
							<h3>Skate App Feed</h3>
							<%= render 'shared/feed' %>
						</div>
					</div>
  </div>
<% end %>
