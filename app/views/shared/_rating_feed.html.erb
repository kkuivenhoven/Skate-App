<% if @rating_items.any? %>
  <ol class="ratingStyle">
    <%= render @rating_items %>
  </ol>
  <%= will_paginate @rating_items %>
<% else %>
	<p>No one <%= @user.name %> is following has rated any spots.</p>
<% end %>
