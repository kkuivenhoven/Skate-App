<!DOCTYPE html>
<!--<div class="btn-group btn-group-justified">
   % if !logged_in? %>
    <div class="btn-group">
       %= link_to('Log in', login_path, class: 'btn btn-primary') %>
     </div>
   % end %>
  <div class="btn-group">
     %= link_to('Skate Spots', skate_spots_path, class: 'btn btn-primary') %>
   </div>
  % if !logged_in? %>
    <div class="btn-group">
       %= link_to('Create Account', new_user_path, class: 'btn btn-primary') %>
     </div>
  % end %>
</div>-->

<!--<br><br><br><br><br><br><br>-->
<!--<br><br><br><br><br><br><br>-->

<% provide(:title, "Home") %>
<% provide(:p, " Created by a skater, for skaters.") %>

  <!--<h4>
   - When submit the form, the view rendered will be the index view of our articles resource
     %= form_tag(skate_spots_path, :method => "get", class: "navbar-form", id: "search-form") do %>
       <div class="input-append">
         %= text_field_tag :search, params[:search], class: "span2", placeholder: "Search Skate Spots" %>
          -In order to have the "search" icon int the button, we need to use plain HTML instead 
             of using a Rails form helper 
         <button class="btn btn-default btn-sm" type="submit"><i class="icon-search">Search</i></button>
       </div>
     % end %>
  </h4>-->

  <!--% if !logged_in? %>
    %= link_to new_user_path, class: 'btn btn-md btn-primary' do%>Create an account% end %>
  % end %>-->



