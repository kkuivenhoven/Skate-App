<!--div class="btn-group btn-group-justified">
  <div class="btn-group">
    %= link_to('Back', skate_spots_path, class: 'btn btn-primary') %>
  </div>
</div-->
<% provide(:title, "New Organization") %>
<% provide(:p, "Create a new organization to inform others!") %>

<%= render 'form' %>

