<p id="notice"><%= notice %></p>

<!--% content_for :head do %>
  %= javasccript_include_tag 'skate_spots.js' %>
 end %> -->

<!--<div class="btn-group btn-group-justified">
  <div class="btn-group">
    %= link_to('Home', root_path, class: 'btn btn-primary btn') %></button>
  </div>
  <div class="btn-group">
    %= link_to('New Skate Spot', new_skate_spot_path, class: 'btn btn-primary btn') %></button>
  </div>
</div>-->

<!--% @skate_spots.each do |skate_spot| %>
  <div class="skate-spot">
    <h1 class="skate-spot-name">%= link_to "#{skate_spot.name}", skate_spot %></h1>
    <p class="skate-spot-zip-code">%= skate_spot.zip_code %></p>
  </div>
 end %>-->
<% provide(:title, "Documented") %>
<% provide(:p, "The streets spots are ordered (reverse) alphabetically.") %>

<div class="container">
  <div class="table-responsive">
    <table class="table table-hover">
     <thead>
       <tr>
        <th>Name</th>
        <th>Zip Code</th>
       </tr>
     </thead>

     <tbody>
      <% @skate_spots.each do |skate_spot| %>
      <tr>
        <tr>
          <td>
            <li class="dropdown">
              <%= link_to "#{skate_spot.name}", skate_spot %>
            <% if logged_in? %>
           <% if current_user.id == skate_spot.user_id %>
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                <b class="caret"></b>
              </a>
              <ul class="dropdown-menu">
                <!--li>%= link_to "Edit", edit_skate_spot_path(skate_spot) %></li-->
                <li><%= link_to "Destroy", skate_spot, method: :delete, data: { confirm: 'Are you sure?' } %></li>
              </ul>
           <% end %>
           <% end %>
            </li>
          </td>
          <td><%= skate_spot.zip_code %></td>
        </tr>
      </tr>
     <% end %>
   </tbody>
  </table>
 </div>
</div>

<br>



