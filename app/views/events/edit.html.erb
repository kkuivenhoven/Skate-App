<p id="notice"><%= notice %></p>

<% provide(:title, "#{@event.name.capitalize}") %>
<% provide(:p, "You are editing the #{@event.name.capitalize} event.") %>
<% provide(:text, "<span style='color: #f4ffe3'>If you do not want to edit the event, please click the 'Back' button.</span>".html_safe) %>


<div class="btn-group btn-group-justified">
  <div class="btn-group">
    <%= link_to('Home', root_path, class: 'btn btn-primary') %>
  </div>
  <div class="btn-group">
    <%= link_to('Back', events_path, class: 'btn btn-primary') %>
  </div>
</div>

<%= render 'form' %>


