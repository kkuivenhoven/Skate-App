<% provide(:title, "New Rating") %>
<% provide(:p, "#{@skate_spot.complete_address}") %>

<div class="btn-group btn-group-justified">
  <div class="btn-group">
    <%= link_to('Home', root_path, class: 'btn btn-primary') %>
  </div>
  <div class="btn-group">
    <!-- /skate_spots/:skate_spot_id/ratings/ -->
    <%= link_to('Back', skate_spot_ratings_path(@rating.skate_spot), class: 'btn btn-primary') %>
    <!-- instead of: %= link_to 'Back', ratings_path %>-->
  </div>
</div>

<%= render 'form' %>

